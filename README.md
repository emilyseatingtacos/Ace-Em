# Ace-Em

Master code

## Context7 MCP
Use the Codex CLI to register the Context7 MCP server so it is available as a tool:

```bash
codex mcp add context7 -- npx -y @upstash/context7-mcp
```

See [context7-mcp-setup.md](./context7-mcp-setup.md) for prerequisites and usage tips.
